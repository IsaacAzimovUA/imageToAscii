services:
  app:
    build:
      context: ./app
    ports:
      - 8000:8000
    volumes:
      - ./app:/app
      - /app/node_modules
  api:
    build:
      context: ./api
      args:
        - API_PORT=${API_PORT}
    ports:
      - '${API_PORT}:${API_PORT}'
    volumes:
      - ./api:/api
