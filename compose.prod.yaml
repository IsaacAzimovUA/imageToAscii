services:
  app:
    build:
      context: ./app
      args:
        - API_PORT=${API_PORT}
    ports:
      - '${APP_PORT}:3000'
    restart: unless-stopped
  api:
    build:
      context: ./api
    ports:
      - '${API_PORT}:3001'
    restart: unless-stopped
